parasails.registerPage('edit-sensor', {
  //  ╦╔╗╔╦╔╦╗╦╔═╗╦    ╔═╗╔╦╗╔═╗╔╦╗╔═╗
  //  ║║║║║ ║ ║╠═╣║    ╚═╗ ║ ╠═╣ ║ ║╣
  //  ╩╝╚╝╩ ╩ ╩╩ ╩╩═╝  ╚═╝ ╩ ╩ ╩ ╩ ╚═╝
  data: {
    syncing: false,
    cloudError: '',

    formData: {
      deviceId: undefined,
      name: '',
      locationDescription: '',
      unit: '',
      measurementDisplayName: '',
      id: '',
      minReading: undefined,
      maxReading: undefined,
    },

    formRules: {
      name: { required: true },
      unit: { required: true },
      minReading: { required: true },
      maxReading: { required: true },
    },

    formErrors: {},

    units: [],
  },

  //  ╦  ╦╔═╗╔═╗╔═╗╦ ╦╔═╗╦  ╔═╗
  //  ║  ║╠╣ ║╣ ║  ╚╦╝║  ║  ║╣
  //  ╩═╝╩╚  ╚═╝╚═╝ ╩ ╚═╝╩═╝╚═╝
  beforeMount: function() {
    // Attach any initial data from the server.
    _.extend(this, SAILS_LOCALS);
    _.extend(this.formData, this.sensor);
    this.formData.deviceId = this.device.deviceId;
    console.log('this.formData', this.formData);
  },
  mounted: async function() {
    //…
  },

  //  ╦╔╗╔╔╦╗╔═╗╦═╗╔═╗╔═╗╔╦╗╦╔═╗╔╗╔╔═╗
  //  ║║║║ ║ ║╣ ╠╦╝╠═╣║   ║ ║║ ║║║║╚═╗
  //  ╩╝╚╝ ╩ ╚═╝╩╚═╩ ╩╚═╝ ╩ ╩╚═╝╝╚╝╚═╝
  methods: {
    submittedForm: function() {
      // Redirect to the sensor detail page on success.
      // > (Note that we re-enable the syncing state here.  This is on purpose--
      // > to make sure the spinner stays there until the page navigation finishes.)
      this.syncing = true;
      window.location = '/devices/'+encodeURIComponent(this.device.deviceId)+'/sensors/'+encodeURIComponent(this.sensor.id);
    },
  }
});
